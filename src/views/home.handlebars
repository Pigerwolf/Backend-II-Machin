<div class="contenedorProductos">
      {{#each productos}}
        <div class="card">
            <p>{{this.title}}</p>
            <p> {{this.price}}</p>
            <p> {{this.description}}</p>
            <button class="btn-compra">Comprar</button>
        </div>
    {{/each}}

</div>

