{{!-- Ejemplo de Ciclos --}}

    {{#each productos}}
    <div class="card">
        <p>{{this.title}}</p>
        <p> {{this.price}}</p>
        <p> {{this.description}}</p>
        <button>Comprar</button>
    </div>
    {{/each}}
