{{!-- Ejemplo de Ciclos --}}

{{#each arrayProductos}}
<div>

    <h2>Titulo: {{this.tittle}} </h2>
    <p>Descipción: {{this.description}} </p>
    <p>Precio: {{this.price}} </p>

</div>

{{/each}}
